@{
  Layout = "_Layout";
}

@model Library.Models.Copy

<h2>Are you sure you want to delete this?</h2>

@Html.DisplayNameFor(model => model.CopyName): @Html.DisplayFor(model => model.CopyName)
@using (Html.BeginForm())
{
    <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to copies", "Index", null, null, new { @class = "btn btn-success btn-sm"})